# Research media

Place topic media in these folders:

- `heave-plates/`
- `floating-renewables/`
- `offshore-safety/`
- `marine-systems/`

Use descriptive lowercase filenames, for example `forced-oscillation-tank.jpg`.
Each topic page contains an HTML comment showing the image and caption markup.
